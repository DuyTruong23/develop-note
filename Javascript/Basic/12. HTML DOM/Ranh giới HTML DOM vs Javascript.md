HTML DOM không phải của JS, mà JS cung cấp bộ phương tiện truy xuất và HTML DOM

JS: Có thể chạy trên Browser | Server(NodeJS)

Browser: HTML → DOM → DOM/WEB API(Application Programing Interface) ⇒ DOM là WEB API